services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: remedygraph
      POSTGRES_PASSWORD: remedygraph
      POSTGRES_DB: remedygraph
    ports:
      - "5432:5432"
    volumes:
      - remedygraph_pg:/var/lib/postgresql/data

volumes:
  remedygraph_pg:
